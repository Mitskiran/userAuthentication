<!-- ejs is basically HTMl file. 
1. called when required for server side rendering that is view result on server side itself.


 2.calling process 
    a. need ejs templating, need path module to resolve the model path.
    
    b. Syntax :- app.set("view engine","ejs"); basically calls ejs as a templating engine.
         require path built in module. 
         app.set("views",path.resolve("./model")); 
         tells node.js that where the views are located. -->
